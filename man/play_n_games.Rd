% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monty-hall-pkg.R
\name{play_n_games}
\alias{play_n_games}
\title{Play the game many times.}
\usage{
play_n_games(n = 100)
}
\arguments{
\item{n}{a number giving how many games to play. Defaults to 100.}
}
\value{
The function returns a data frame with \code{2 * n} rows and 2
columns holding the \code{strategy} and \code{outcome} of every game. It
also prints a table of win and lose proportions by strategy,
rounded to 2 decimal places.
}
\description{
\code{play_n_games()} plays the game \code{n} times, keeps the results of
every game, and prints the share of wins and losses for each
strategy.
}
\details{
One game is not enough to tell which strategy is better, so this
loops through the game many times and stacks all the results. The
more games you run, the closer the win rates get to 0.33 for stay
and 0.67 for switch.
}
\examples{
  results <- play_n_games( n=100 )

}
